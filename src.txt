//comment demonstration

main() {
    testClass = class

    testClass.string = "a string"
    testClass.int = 127
    testClass.innerClass = class
    testClass.innerClass.float = 0.1f

    func(testClass.string, testClass.int, testClass.innerClass.float)
}

func(s, i, f) {
    print(s, i, f)
}